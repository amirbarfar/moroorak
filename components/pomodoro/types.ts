export interface StatItem {
  label: string;
  value: string;
}

export interface FocusTip {
  title: string;
  description: string;
}

export interface TimerStats {
  focusHours: string;
  completed: string;
  flowState: string;
}